1.linux系统下面切换账号命令:su root
2.查看当前所在位置命令:pwd
3.创建文件夹命令:mkdir
4.删除文件命令:rm -f 文件名
5.删除文件夹命令:rm -rf 文件夹名
6.改变文件名称命令:mv 原来的文件名称  新的文件名称
7.查看当前路径下的所有文件命令:ls -a (这里注意:一般以.开头的文件默认在Xshell工具下是看不到的)
8.第一次使用vim,终端左下角会提示E353:Nothing in register这个时候只需按i或者a进入vi的
     插入模式就可以进行编辑了(编写完成后记住按esc退出编辑并且: wq命令保存)